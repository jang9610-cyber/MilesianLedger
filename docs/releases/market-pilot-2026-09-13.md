# 시장 통계 Worker 배포 · 2026-09-13

기존 교역 시세 Worker에 시장 통계 모듈과 SQLite Durable Object `MarketCollector`를 추가했습니다. GitHub 베타 Release와 기존 교역 조회 경로는 유지합니다. 새 넥슨 키 발급·교체 또는 Cloudflare 요금제 변경은 하지 않았습니다.

## 배포 상태

- Worker: `restless-bread-9002milesianledger-api`
- 배포 전 버전: `425b524e-c35e-4d35-8697-db9600343016`
- 최종 변경 버전: `d0af3145-7236-427d-99c9-f561029b1a78`
- 기존 교역: `AUCTION_ENABLED=true`, 공용 예산 10,000회/24시간 유지.
- 시장 조회: `MARKET_ENABLED=true`.
- 정기 수집: `MARKET_SCHEDULE_ENABLED=false`. Cron은 등록되어 있지만 이 설정에서는 작업을 시작하지 않습니다.
- 관리자 시범 실행용 Secret `MARKET_ADMIN_TOKEN` 추가. 값은 앱·Git에 포함하지 않습니다.

## 시범 실행

실행 ID `pilot-20260913-a`, 2026-09-13 17:09:05~17:09:12 KST, 약 7.6초.

| 항목 | 결과 |
| --- | --- |
| 거래 내역 | 4페이지, 2,000건 수신·중복 제거 저장 |
| 현재 매물 | 4페이지, 2,000행 수신·검증 |
| 정상 페이지 조회 | 합계 8회 |
| 저장소 파일 크기 | 385,024바이트, 약 376 KiB |
| 종료 이유 | 지정된 4페이지 상한 도달 |

두 스트림 모두 다음 페이지가 남아 있어 전체 수집 완료로 처리하지 않았습니다. 상태 `limited`와 코드 `MARKET_PILOT_LIMIT`은 계획된 시범 종료이며 API 장애가 아닙니다. 미완료 매물 집계는 공개하지 않고 정리했으며, 거래 원장 2,000건은 관측 통계에 남겼습니다. 통계 조회 결과는 시장 전체의 인기 순위로 해석할 수 없습니다.

기존 거미줄 조회에서 HTTP 200 및 매물 78행을 확인했습니다. 통계 읽기 경로와 관리자 지표 경로도 정상 응답했습니다. 인증 없는 관리자 실행은 HTTP 401로 거부했습니다. 같은 실행 ID를 재사용해도 새 수집을 시작하지 않습니다.

## 검증 및 다음 단계

오프라인 서버 테스트 40개와 앱 화면 검증이 통과했습니다. 밝은 테마·다크모드, 갱신 실패 시 이전 목록 유지, 시범 데이터 안내를 포함합니다.

무료 플랜의 SQLite 일일 쓰기 한도에 맞추기 위해 정기 전체 수집은 꺼둡니다. 묶음 저장 및 집계 방식으로 쓰기량을 줄인 뒤 전체 한 차례의 비용과 데이터 범위를 다시 측정해야 합니다. 부분 표본에서 전체 시장 수집 비용을 확정하지 않습니다.

시범 기록 파일은 배포 작업 폴더의 `artifacts/market-live-20260913`에 보관합니다. 수집을 모두 중지하려면 새 DB를 삭제하거나 예전 버전으로 되돌리기보다 `MARKET_ENABLED=false`로 같은 시장 설정을 재배포합니다.
