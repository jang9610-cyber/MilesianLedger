# 평균 단가 분류 정책 — 2026-09-14

개발 빌드 `1.1.0-dev.1`의 평균 비교 대상을 고정된 재료 허용 목록에서 유동 옵션 분류 제외 방식으로 변경했습니다. 분류의 정확한 범위는 [시장 통계 문서](../MARKET_STATISTICS.md#평균-단가의-분류-기준)에 있습니다.

## 서버와 기존 자료

- Cloudflare Worker 버전: `ca6775fb-87a4-4246-a24c-8e15a243f447`.
- 정책 버전: `category-prices-v2`.
- 기존 수집 주기, API 예산, Secret, Durable Object binding은 유지했습니다.
- 원래 저장된 거래 청크의 비교 플래그 대신 현재 분류 기준으로 평균을 계산합니다. 과거 청크나 거래 ID를 재작성하지 않습니다.
- 게시 서명에 정책 버전을 포함해 다음 정기 처리에서 동일 수집본도 새 정책으로 게시할 수 있습니다. 별도 전체 재수집은 실행하지 않았습니다.

## 앱과 실제 공개 데이터 검증

앱은 기존 gzip 저장본을 읽을 때 분류 규칙을 적용합니다. 과거에 평균을 숨긴 고정 품목은 금액 합계와 거래 수량으로 복원하고, 유동 옵션 분류의 평균은 숨깁니다. 새 정책을 적용한 뒤에도 최저 매물가는 별도로 표시하고 5배 위험군 판정을 유지합니다. 원본 파일·해시·집계 합계·수집 시각은 그대로입니다.

실제 공개 게시본 `d08adc62ada32e29181a4959589566f304d7dc77acc82a9cdb246a99cf9ce735`는 2026-09-14 09:00:26 KST에 생성된 기존 정책 데이터입니다. 이 파일의 해시·압축 크기를 확인하고 새 앱으로 읽어 다음을 검증했습니다.

| 항목 | 결과 |
| --- | ---: |
| 기간별 품목·분류 조합 | 16,805개 |
| 새 앱에서 표시 가능한 24시간 평균 | 3,349개 |
| 유동 옵션 분류 품목 | 8,261개 |
| 교역 품목 검증 | 117개 성공, 실패 0개 |
| 가격이 있는 시장 검색 이름 | 16,464개 |
| 이름이 식별된 인챈트 검증 | 715개 |

거래가 없는 품목에는 평균을 만들지 않습니다. 이름을 식별하지 못한 인챈트의 혼합 평균·최저가도 숨깁니다. 공개 파일 최초 확인은 manifest와 gzip 2회, 같은 버전의 교역 갱신은 조건부 확인 1회였으며 품목 검색은 추가 HTTP 요청 없이 처리했습니다.

서버 테스트 95개, 앱 정책 검증 322개(서버 제외 분류 52개와 일치 검증 포함), 스냅샷·검색·통계 회귀 검사와 시장 통계·수수료 분배 UI 검증을 통과했습니다. UI 검증에는 기존 고정 품목 평균 복원, 토템 평균 미표시, 인챈트 이름 미확인 설명, 복원된 평균의 5배 위험군 분류가 포함됩니다.
