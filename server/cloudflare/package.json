{
  "name": "milesian-ledger-cloudflare-proxy",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test *.test.mjs",
    "dev": "wrangler dev --config wrangler.market.jsonc",
    "check": "wrangler deploy --dry-run --config wrangler.market.jsonc --outdir .dry-run-market",
    "deploy": "wrangler deploy --config wrangler.market.jsonc",
    "check:market": "wrangler deploy --dry-run --config wrangler.market.jsonc --outdir .dry-run-market",
    "dev:market": "wrangler dev --config wrangler.market.jsonc --test-scheduled"
  },
  "devDependencies": {
    "wrangler": "4.131.0"
  }
}
