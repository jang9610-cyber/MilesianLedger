{
  "name": "milesian-ledger-cloudflare-proxy",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test worker.test.mjs",
    "dev": "wrangler dev",
    "check": "wrangler deploy --dry-run --outdir .dry-run",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.131.0"
  }
}
